import { Component } from '@angular/core';

/**
 * PTO Layout Component
 *
 * Wrapper component that renders the sub-navigation bar at the top
 * and a router-outlet for the active child route below.
 */
@Component({
  selector: 'app-pto-layout',
  templateUrl: './pto-layout.component.html',
  styleUrls: ['./pto-layout.component.scss']
})
export class PtoLayoutComponent {}
